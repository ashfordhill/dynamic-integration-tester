// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark', // Enables dark mode
    // Optional: Customize additional palette settings
    // primary: {
    //   main: '#90caf9',
    // },
    // secondary: {
    //   main: '#f48fb1',
    // },
  },
  // Optional: Customize other theme aspects (typography, spacing, etc.)
});

export default theme;